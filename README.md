# DodgeDash

躲避游戏：控制角色闪避下落/逼近的障碍物，生存越久得分越高。

## 技术栈
- 纯 HTML/CSS/JavaScript（单文件可玩，无需构建）
- Canvas 渲染

## 运行
直接在浏览器打开 `index.html`，或 `python3 -m http.server` 后访问。

## 玩法
- 方向键/WASD 移动角色
- 躲避红色障碍物
- 得分随时间增长，碰撞结束

## 开发
编码由 codex CLI 完成：`codex exec "..."`（在仓库目录内）。
