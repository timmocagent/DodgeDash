# DodgeDash 修改清单 v8（锤子稀有化 + 撤离机制）

修改现有 /root/DodgeDash/index.html。完成后 node --check + 无头冒烟测试。

## 核心原则
锤子是**精英怪**，全关**最多出现 2 次**（关卡 1/3 处一次、2/3 处一次），boss 战**绝无锤子**（精英怪和 boss 不同时出场）。其余时间全是小怪波。锤子"不能乱用"——稀有、隆重、有仪式感。

## 1. 锤子时间轴：1/3、2/3 各一次 + boss 压轴
- 替换现有"锤子波↔普通波交替"（updateWaves/onWaveStart/clearNormalMonsters/clearHammers 逻辑）为**固定时间轴**：
  - **0 → T1（关卡前 1/3）**：只有小怪波（黑怪/冲刺怪/史莱姆等按阶段混合），无锤子。
  - **T1（约 elapsed = 关卡总时长 × 1/3，假设总长 ~90s → 约 30s）**：第一次锤子精英登场（2 个，4 列不重叠）。
  - **T1 → T2（中 1/3）**：锤子已撤离后，恢复小怪波。
  - **T2（约 60s）**：第二次锤子精英登场（2 个）。
  - **T2 → T3（后 1/3）**：恢复小怪波。
  - **T3（约 90s，boss 战开始）**：boss 红蛇王登场。**boss 战期间不再出锤子**（hammers 已在第二次锤子撤离后清空，boss 阶段 spawn 逻辑禁止锤子）。
- 删除 waveType/waveTimer 交替机制；用 `hammerSpawns = 0` 计数 + 时间点触发。

## 2. 小怪撤离：撤出屏幕（不是瞬移消失），且提前于锤子到来
- 锤子到来前，场上小怪**开始撤离**：小怪朝屏幕外移动（向上/向侧移出屏幕边缘），**移动出屏幕外才消失**（不是原地消失）。
- 撤离开始时间：锤子到来前约 4~6 秒开始撤离。
- **小怪撤离不阻塞锤子到来**：时间到锤子就登场，即使有小怪还没撤完（锤子和小怪短暂同屏是允许的——锤子会碾压它们，这也是"不分敌我"的体现）。
- 撤离视觉：小怪有撤退动画（快速向屏幕外移动 + 轻微粒子尾迹），不用文字提示。
- 实现：给怪物加 `retreating` 状态，更新循环里 retreating 实体按撤退方向快速移动，出屏幕后移除。锤子时间点触发时先标记全场小怪 retreating。

## 3. 锤子撤离：出屏幕外才消失（非瞬间）
- 锤子完成"砸击→抬升预演→快速拉起"后，**继续向屏幕外移动**（向上移出屏幕顶），移出后才从数组移除（不再原地消失）。
- 撤离期间锤子不再砸击（只上移），无伤害判定。
- 视觉：锤子上移时有向上粒子尾迹，体现"收回"。

## 4. 移除锤子/怪群文字提示，用真空期过渡
- 删除 `showHint('锤兵来袭！')`、`showHint('怪物群 来袭！')`、`showHint('精英锤兵 来袭！')` 等波次文字横幅。
- 过渡方式：**怪物真空期**——小怪撤离后、锤子登场前有 4~6 秒无怪空档（背景继续滚动，星星/网格照常），让玩家喘息并直觉"要来新东西了"；锤子撤离后同样有短暂空档再刷小怪。
- 保留的提示：boss 出场提示（'红蛇王登场…'）、寄生蛆提示、升级球提示、道具提示（这些不是波次提示，保留）。锤子本身的 popup（'精英锤兵 摧毁'）保留（是战斗反馈不是波次预告）。
- 小怪撤离不加文字（popup '怪物群撤离' 删除，改为纯视觉撤退）。

## 5. 锤子 count 上限硬约束
- `hammerSpawns >= 2` 后，任何逻辑（含 spawnHammerElite 被意外调用）都不再生成锤子。
- boss 战（phase 4 / bossDefeated）期间禁止生成锤子。
- 初始小怪波不生成锤子。

## 6. 波次/小怪生成逻辑适配
- 保留 spawnNormalWaveMob 的小怪种类按阶段混合逻辑，但去掉与 waveType 的耦合，改为独立持续生成（小怪持续出现直到触发撤离）。
- updateWaves 改为 `updateLevelFlow(dt)`：
  - 小怪持续生成（间隔 ~2.4-4.2s）。
  - 时间到 T1/T2 → 触发锤子波（先标记小怪撤离 → 4-6s 后 spawnHammerPair）。
  - 锤子撤离完成后恢复小怪生成（有 3-5s 空档）。
  - 时间到 T3 → 清空剩余小怪（快速撤离）+ spawnBoss，之后不再刷小怪（boss 战专注）或少量小怪（可保留，但无锤子）。

## 交付
- 只改 index.html。node --check + 无头冒烟测试（覆盖：锤子全关 ≤2 次且时间点正确、boss 战无锤子、小怪撤离出屏幕才消失、撤离不阻塞锤子、锤子撤离出屏幕才消失、无波次文字提示、死亡重试、胜利）。
- 截图：小怪波、锤子波、真空期、boss 波、移动端。
