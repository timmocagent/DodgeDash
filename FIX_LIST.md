# DodgeDash 修改清单（v3）

修改现有 /root/DodgeDash/index.html（单文件，纯 vanilla JS + Canvas）。保留霓虹暗色视觉风格和整体架构，但按以下 6 点修改。所有改动完成后跑 node --check + 无头冒烟测试。

## 1. 玩家可飞全屏（消除右侧死区）
- 当前 `clampPlayer()` 把玩家 x 限制在 `20 ~ W * 0.6`，导致右边 40% 不可达。
- 改为 `p.x = clamp(p.x, 20, W - 20)`，玩家可飞全屏（y 保持合理范围）。
- 炸弹生成范围、怪物活动范围同步放开到全屏。

## 2. 飞机朝向：朝前（右）飞行
- 横版卷轴世界向右推进，飞机应始终朝右前方飞行。
- 当前 renderPlayer 的三角形朝右，但没有体现"向前飞"的动感：
  - 飞机头部保持朝右（+x 方向），尾焰/推进光效拖在左侧。
  - 根据玩家垂直速度微调机身俯仰（上移时略微抬头、下移时略微低头，clamp ±15°），用 ctx.rotate 实现。
  - 增加朝右的尾焰粒子（引擎喷流），强化"往前飞"的感觉。

## 3. 炸弹爆炸范围缩小 + 玩家判定更宽松
- 当前 `rad = 120 * (1 + 0.3 * bombRadiusUp)` 太大，玩家"躲开也被炸"。
- 改为基础半径 `rad = 72`（升级后最多 ×1.5）。
- 玩家受伤判定与怪物判定分离：
  - 怪物/锤子/炮台判定：`dist < rad + r`（正常范围）。
  - 玩家判定：`dist < rad * 0.55 + player.r`（玩家只有靠近爆炸核心才受伤），并给玩家爆炸伤害 0.35 秒延迟窗口（爆炸发生后先显示冲击波，0.2 秒内玩家若已离开安全区则不受伤——或直接缩小范围即可，二选一，简单优先：缩小范围 + 玩家判定半径收紧）。
- 爆炸视觉（冲击波/粒子）保持与 rad 同步缩放。

## 4. 锤子重构：矩形精英怪，从屏幕外砸下，4 列制
- **形态**：锤子改为矩形（宽约 W*0.22、高约 60 的发光矩形锤头 + 顶部锤柄），从屏幕外（上方）垂直砸下来，落点在地面附近。
- **4 列制**：屏幕竖向 4 等分（每列宽 W/4，列号 0-3）。**同时只有 2 个锤子**，各自占据一列（随机选 2 列，可重叠时重新选）。每个锤子只在它那列范围内砸击（落点 x = 该列中心）。
- **精英怪表现**（不是小怪）：
  - 体型大、有独立血条（显示在锤子上方或屏幕顶部精英槽）。
  - 血条：2 格血，需要 2 次炸弹爆炸才能摧毁（第一次砸裂/变红，第二次摧毁）。被冷冻子弹冻住时停止砸击。
  - 砸地瞬间：全屏微震 + 该列冲击波 + 敲击音效，砸击点对玩家/炸弹/黑怪有伤害判定（同现有逻辑但范围与矩形匹配）。
  - 精英出场提示（顶部横幅 "ELITE HAMMER" → 中文"精英锤兵"）。
- 其他锤子逻辑（被炸弹炸毁、被冷冻暂停、敲击炸弹引爆）保留，但适配矩形碰撞（用落点中心 + 矩形半宽判定）。
- 更新关卡生成逻辑：阶段 1 出 2 个锤子（各占 1 列），后续阶段保持 2 个精英锤兵 + 其他怪物，锤子被摧毁后过一段时间在该列或另一空列重生。

## 5. 升级系统改为"魂斗罗式升级球"（去掉弹窗）
- **删除** upgradeOverlay 弹窗、升级二选一 UI、`state='upgrade'` 暂停流程。删除 `triggerUpgrade/pickUpgrade/upgradeOverlay/upgradeOptionsEl/upgradePending/__pickUp` 相关代码。
- **升级球**：新的地图道具 `powerOrb`——发光圆球（青色/金色交替脉冲），从屏幕上方缓缓落下或从右侧飘入，悬浮在可拾取位置。
- **吃到即升级**：玩家碰到升级球 → 立即随机应用一个可用升级（从 UPGRADES 池随机抽一个 avail 的），显示 popup（升级名称 + 图标），不打断游戏、不弹窗。
- **频率降低**：升级球出现间隔拉长（基础 40~55 秒一个，且每关最多 3 个）。
- **保底机制**：如果某个升级球生成后 25 秒内没被吃到（或已从屏幕消失），下一次升级球"奖励升级"——即保底球吃到后连升 2 级（或必定出现当前最强的可用升级）。界面上保底球用更亮/更大的外观 + 角标"BONUS"（中文"保底"），让玩家知道错过上一个会得到更好的。
- 升级效果保留现有 4 种（速度/生命上限/炸弹强化/护盾），名称中文化。
- 击杀仍然提供少量能量（可在 HUD 显示能量条），但升级只通过升级球触发，不再因为击杀数强制弹窗。

## 6. 全部文字中文化
- 所有 UI 文案改中文（面向中文玩家）：
  - 主菜单标题保留 "DODGEDASH"（品牌），副标题/操作说明/提示全中文。
  - 按钮：开始、暂停、继续、重试、静音、放下炸弹。
  - HUD：分数、时间、阶段、击杀、能量。
  - 提示横幅（阶段切换/精英出场/boss 出场）。
  - 升级名与描述：速度+20%、生命上限+2、炸弹精通（搬运加速+爆炸范围+30%）、护盾 3秒(可升5秒)。
  - popup：拾取炸弹、放下炸弹、被冰冻、-N 生命、精英锤兵被摧毁、黑怪唤醒、保底升级等。
  - 结束/胜利画面：通关、游戏结束、得分、用时、最佳成绩、击杀统计、升级次数、再玩一次。
  - 教程/提示条（"拾取橙色炸弹，放在锤子砸击路径上然后跑开"等）。
  - 冻结/冰冻、伤害翻倍、关卡阶段描述等。
- 注意中文字体：CSS 加 `font-family: 'PingFang SC','Microsoft YaHei',sans-serif` 兜底，避免中文显示为方块。

## 交付
- 只改 index.html。完成后 node --check 验证 JS，无头冒烟测试覆盖：启动、全屏移动、拾取/放下炸弹、锤子砸击引爆、精英锤兵两血摧毁、升级球吃到升级、保底升级、死亡重试、胜利。截图确认移动端 UI 无重叠、中文正常显示。
