# DodgeDash 修改清单 v4（8 项）

修改现有 /root/DodgeDash/index.html（单文件，纯 vanilla JS + Canvas，全中文 UI，霓虹暗色风格）。完成后 node --check + 无头冒烟测试 + 截图（含移动端）。

## 1. 锤子更重、更慢、更低频
- 锤兵下落速度明显降低（比当前慢约 40%），下落过程中有明显的"沉重"感（下落阶段可加轻微屏幕震动/风声）。
- 锤子尺寸再加大（更庞大的工具感）。
- 锤兵攻击频率降低：下落→砸地→抬起的完整周期拉长 1.5~2 倍；重生间隔也拉长。

## 2. 锤子推动玩家机制 + 伤害规则（关键）
- 锤子下落过程中如果碰到玩家，**推着玩家一起下移**到锤子的底部落点（玩家被锤头推着走，无法穿过去）。
- 伤害规则（任何情况下单次锤子事件最多扣 2 血，不存在 3 血）：
  - 锤子下落碰到玩家的瞬间：扣 1 血。
  - 玩家被推到锤子落点底部时：再扣 1 血（共 2 血）。
  - 如果玩家本来就在底部（落点区域）：锤子砸下直接扣 2 血（没有两次判定，就是一次扣 2）。
  - 玩家被推动期间若因其他原因扣血，仍遵守"锤子事件总共不超 2 血"（实现上：锤子事件用一个标记，同一次锤击只允许扣 2 血封顶，可把第一次 1 血 + 到底 1 血合并，或用"本次锤击已扣血量"计数器 clamp 到 2）。
- 玩家被推到底后锤子砸地，锤子进入抬起阶段，玩家恢复自由。

## 3. 道具刷新率按玩家状态动态调整
- 新增道具类型概念（升级球之外的可拾取道具）：
  - 回血道具（+1 血/红心）
  - 护盾道具（3 秒无敌）
  - 速度提升（短暂加速或永久速度+）
  - 炸弹容量（可同时携带炸弹 +1）
- 动态权重：
  - 玩家血量 < 50%（hp/maxHp < 0.5）：回血道具刷新概率大幅提高（约 50%）。
  - 玩家血量充足（>=50%）但护盾未持有：护盾道具概率提高（约 40%）。
  - 两者都健康（hp 满/接近满 且有护盾）：速度提升 + 炸弹容量道具概率提高（各约 30%）。
- 实现：pickup 生成时根据当前玩家状态计算各类型权重，加权随机选一个。

## 4. 视角改为纵版：飞机朝前（上）飞，背景向后滚动（大改）
- **不再朝右**。改为纵版飞行（像经典飞行射击：飞机在下方，朝上飞，背景向下滚动表现前进）。
- 布局调整：
  - 玩家位置：屏幕下方区域（y 约 H*0.75 附近），可全屏移动。
  - 背景：星星/网格/地面纹理持续向下滚动（速度恒定，象征向前飞）。
  - 怪物/锤兵/道具：从屏幕上方出现，向下运动（锤兵从屏幕外上方砸下来）。
  - 黑怪从上方掉落；冷冻枪在上方或侧面；boss 从上/侧进入。
- 所有坐标逻辑（生成、移动、碰撞）按纵版重新映射：y 变小=向上，y 变大=向下。飞机渲染：机头朝上（-y），尾焰朝下。
- 控制不变：WASD/方向键 + 触屏拖动，上=前进（背景滚更快/视觉上）或保持等速滚动即可。
- 画面滚动与"放下炸弹"的交互见第 5 点。

## 5. 放下后的炸弹固定不随画面滚动
- 玩家放下炸弹后，炸弹**固定在放下时的屏幕位置**，不再随背景滚动、不再自行漂移（vx/vy 清 0）。
- 未拾取的炸弹也不随背景滚动（它们本来就在屏幕坐标系里生成，只需确认不叠加滚动偏移）。
- 玩家搬运中的炸弹跟随玩家。

## 6. 冷冻枪节奏更慢：冷却长、瞄准 3 秒、冷却期无激光
- 行为序列：进入冷却（长，约 8~10 秒，期间完全不显示瞄准激光/红线）→ 瞄准（3 秒，显示旋转瞄准激光/红线锁定玩家）→ 发射 1 发慢速冷冻子弹 → 回到冷却。
- 发射频率整体更低（周期约 11~13 秒一次）。
- 冷冻子弹效果不变（冻住一切，受击伤害翻倍）。

## 7. Boss 修正：接触伤害 + 蛇形转向限制
- Boss（红怪蛇）**增加接触伤害**：玩家碰到蛇的任何节 → 扣 1 血（带击退和无敌帧，和普通怪物接触伤害一致）。
- 转向像蛇：每前进一段距离只能转向一定角度——实现为蛇头每移动固定距离（如每 30px）只能改变最多一定角度（如 12°），且转向有惯性（目标角度与当前角度差被 clamp）。禁止瞬移/急转。
- 速度降低（比当前慢约 25%）。
- 身体节段跟随头部平滑移动，保持蛇形。

## 8. 移动端双指释放炸弹
- 保留现有"放下炸弹"按钮。
- 新增手势：**一根手指在屏幕上拖动移动时，出现第二根手指（任意位置）→ 立即释放炸弹**（不需要碰到按钮）。
- 实现：pointer 事件里跟踪活动指针数；检测到 second pointerdown 且 first 正在拖动 → 若玩家携带炸弹则 dropBomb()，并短暂提示"已放下炸弹"。
- 注意不要与双指缩放/滚动冲突（touch-action: none 已有）。

## 交付
- 只改 index.html。node --check + 无头冒烟测试（覆盖：锤子推动+2血规则、动态道具权重、纵版滚动、炸弹固定、冷冻枪新节奏、boss 接触伤害+蛇形转向、双指释放炸弹、死亡重试、胜利）。
- 截图：桌面菜单/游戏中、移动端（确认按钮 + 中文无溢出）。
