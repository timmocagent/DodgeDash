# DodgeDash 修改清单 v6（试玩反馈 3 项）

修改现有 /root/DodgeDash/index.html。完成后 node --check + 无头冒烟测试 + 截图。

## 1. 通关条件：boss 死亡即通关（冷冻枪不算必须清理）
- 当前胜利条件可能是"boss + 锤子 + 冷冻枪全清"。改为：**boss 死亡即胜利**。
- 冷冻枪不是必须清理的敌怪（可以留着不打，不影响通关）。
- 锤子同理不阻塞通关（boss 死即过关）。
- 胜利画面统计保留。

## 2. 锤子动作重做：加速冲击 + 底部骤停 + 抬升预演 + 快速拉起
- 当前锤子下落是匀速。重做锤子下落/抬起的时间曲线：
  1. **下落（冲击）**：加速下落，越到底越快（加速度曲线，如 t² 或 ease-in 强曲线），体现"重锤砸下"的冲击力。
  2. **底部骤停**：到达底部落点瞬间直接停住（硬停，伴随大震屏 + 冲击波 + 重击音效）。
  3. **抬升预演**：先缓慢抬起一小截（约锤头高度 15~20%），然后**停顿**片刻（0.25~0.4 秒，制造"即将抬起"的蓄力感，玩家可以趁机从被压状态逃跑）。
  4. **快速拉起**：停顿后，以相对较快的速度把锤子拉起（回到初始高度，为下一次下落做准备）。
- 总周期比当前略长（因为加了预演停顿），但拉起阶段更快。手感目标："砸下来很重、很慢地开始抬、蓄力一瞬、快速抽走"。
- 被压的玩家/炸弹/怪物在底部停留时间相应延长（直到锤子进入快速拉起阶段才释放？——不，**到底部骤停后就释放被压实体**（锤子砸地瞬间结算伤害并释放），抬升预演阶段它们已自由，只是锤子还在底部附近制造视觉压迫）。

## 3. 锤子四列不重叠（可留空隙，但绝不重叠）
- 锤子列选择 bug：两个锤子的攻击范围重叠了。
- 修复：4 列分配保证**任意两个锤子的矩形攻击范围互不重叠**。
- 可以不完全覆盖屏幕（允许列间留空隙），但必须零重叠。
- 实现：锤子列宽 = min(屏幕四等分宽, 允许的锤宽上限)；选列时保证 `hammerColLeft[i] + hammerWidth <= hammerColLeft[j]`（对任意 i<j），即按左边界+宽度排序检查。如果随机选到重叠组合就重新选（重试上限内）。

## 交付
- 只改 index.html。node --check + 无头冒烟测试（覆盖：boss 死即通关（冷冻枪/锤子残留不影响）、锤子四段动作时间线、列不重叠断言（多次随机 spawn 无重叠）、被压释放时机、死亡重试）。
- 截图：锤子下落帧/底部骤停帧/抬升预演帧/快速拉起帧/移动端。
